{
  "name": "hardhat-project",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "hardhat": "^2.13.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "dotenv": "^16.0.3"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test test/SUCoin.ts",
    "console": "hardhat console",
    "localnode": "hardhat node",
    "localdeploy": "hardhat run scripts/deploy.ts --network localhost",
    "verify-goerli": "hardhat verify --network goerli <address> <unlock time>$"
  }
}
